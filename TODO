lib:

expose function to expand hands from hand_range
function to convert between list of ratios and percentages

handle 0depth folder equilab

gtoplus - 

figure out hand order

app:

find a repl lib
